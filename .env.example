# Rename this file to .env and provide the MongoDB connection string.
# If a connection string is not provided, the backend falls back to an in-memory MongoDB instance for development.
MONGODB_URI=mongodb://127.0.0.1:27017/comradeBakery
PORT=5000
